{
  "name": "manavarya-bot",
  "version": "0.1.0",
  "private": true,
  "description": "AI PR reviewer that uses a local Ollama model (qwen2.5-coder).",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --minify --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "local": "ts-node src/cli.ts"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@vercel/ncc": "^0.38.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
